import { Text } from 'react-native';

import { colors } from '@/theme';

export function AdminCoordinateMap() {
  return (
    <Text style={{ color: colors.secondaryLabel }}>
      El selector cartográfico administrativo está disponible en la versión web.
    </Text>
  );
}
